; This checks for signature by the transfer key

Context: ACCOUNT
{
    pick 1          ; get account struct on top of stack
    get TRANSFER_KEY
    pick 1          ; now get transaction
    get HASH        ; get the transaction hash
    pick 2          ; get tx again
    get SIGNATURE   ; get the transaction signature
    verify          ; verify(signature, message, key)
}
